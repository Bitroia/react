import React from 'react'

const ChangeMessage = ({handle}) => {
    const messages = ["oi", "olá", "tudo bem?"]
  return (
    <div>
        <button onClick={}></button>
    </div>
  )
}

export default ChangeMessage